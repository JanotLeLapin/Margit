--- LocaleLanguage.java
+++ LocaleLanguage.java
@@ -22,7 +22,7 @@
 
     public LocaleLanguage() {
         try {
-            InputStream inputstream = LocaleLanguage.class.getResourceAsStream("/assets/minecraft/lang/en_US.lang");
+            InputStream inputstream = LocaleLanguage.class.getResourceAsStream("/mojang-translations/en_US.properties"); // Margit
             Iterator iterator = IOUtils.readLines(inputstream, Charsets.UTF_8).iterator();
 
             while (iterator.hasNext()) {
@@ -41,7 +41,7 @@
             }
 
             this.e = System.currentTimeMillis();
-        } catch (IOException ioexception) {
+        } catch (Exception ioexception) { // Margit
             ;
         }
 
